&simulation_params
    nparticles = 100,
    ndumpfiles = 1
    nchains = 10,
    nbeads = 10,
    dt = 0.01,
    nframes = 100,
    dump_freq = 100,
    is_cubic = .true.
    dumpfilenames = "../data/with_molid.lammpstrj"
/

&Function1DInfo
    is_log = .true.
    npoints = 20
    !base = 2
    !window_width = 10
/
&simulation_params
    nparticles = 100,
    ndumpfiles = 2
    nchains = 10,
    nbeads = 10,
    dt = 0.01,
    nframes = 200,
    dump_freq = 100,
    is_cubic = .true.
    dumpfilenames = "../data/with_molid.lammpstrj", "../data/simple.lammpstrj"
/
