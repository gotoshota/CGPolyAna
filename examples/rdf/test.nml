&MDParams_nml
    dumpfilenames = "prod.lammpstrj"
    ndumpfiles = 1
    nchains = 100,
    nbeads = 50,
    dt = 0.01,
    nframes = 101
    dump_freq = 10000,
    is_cubic = .true.
/

&Function1Dinfo
    is_log = .true.
    npoints = 20
    !base = 1.5
    !window_width = 10
/
