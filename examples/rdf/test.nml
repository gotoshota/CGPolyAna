&MDParams_nml
    dumpfilenames = "dump.lammpstrj"
    ndumpfiles = 1
    nchains = 100,
    nbeads = 400,
    dt = 0.01,
    nframes = 10000
    dump_freq = 1,
    is_cubic = .true.
/

&Function1Dinfo
    is_log = .true.
    npoints = 20
    !base = 1.5
    !window_width = 10
/
