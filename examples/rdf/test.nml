&MDParams_nml
    dumpfilenames = "dump.lammpstrj"
    ndumpfiles = 1
    nchains = 100,
    nbeads = 400,
    dt = 0.01,
    nframes = 100
    dump_freq = 1,
    is_cubic = .true.
/

&TimeDependentFunctionInfo
    is_log = .true.
    npoints = 20
    !base = 2
    !window_width = 10
/
